#!/bin/bash

TIME=`date +%y%m%d`

FOLDERNAME=`basename $1`



    tar -cpzf /Volumes/BACKUP1/${TIME}.${FOLDERNAME}.tar -C $1 .
    echo Finished backing up $FOLDERNAME in /Volumes/BACKUP1/${TIME}.${FOLDERNAME}.tar
