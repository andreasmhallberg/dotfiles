% Inputted in YAML-header with `header-includes: \input{\string <path to this file>}

% Clean tables

\usepackage{booktabs}
\renewcommand\toprule{\null}
\renewcommand\midrule{\null}
\renewcommand\bottomrule{\null}

\renewcommand{\textbf}[1]{\structure{#1}} 

% Column environments that avoid \begin commands.

% Kept for backwards compability. Use :::::: spans from pandoc markdown instead

\newcommand{\columnsbeginT}{\begin{columns}[T]} % Align with top baseline
\newcommand{\columnsbegint}{\begin{columns}[t]} % Align with top.
\newcommand{\columnsbegin} {\begin{columns}} % Default c.

\newcommand{\columnsend}   {\end{columns}}

  % Start column within env
  \newcommand{\col}{\column{.5\textwidth}}
  \newcommand{\colthird}{\column{.33333\textwidth}}
  \newcommand{\coltwothirds}{\column{.666666\textwidth}}
  \newcommand{\colfourth}{\column{.25\textwidth}}

% \hfill for frametitles
  \newcommand{\fhfill}{\hspace{0pt plus 1 filll}}


% navigation symbols
\addtobeamertemplate{navigation symbols}{}{%
 \usebeamerfont{footline}%
 \usebeamercolor[fg]{footline}%
 \hspace{1em}%
 \insertframenumber/\inserttotalframenumber
}

% better list symbols
\setbeamertemplate{itemize item}{\Large-}
\setbeamertemplate{itemize subitem}{\Large-}
\setbeamertemplate{itemize subsubitem}{\Large-}


\setbeamercolor{quotation}{fg=gray!80}
